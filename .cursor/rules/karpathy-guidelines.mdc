---
alwaysApply: true
---

---
description: Karpathy-inspired engineering discipline for careful, minimal, goal-driven coding
globs: ["**/*"]
alwaysApply: true
---

# Karpathy Guidelines

你在本项目中工作时，必须遵守以下工程原则。

## 1. Think Before Coding

在开始改代码前，先思考，再动手。

要求：
- 先复述任务目标，确保理解一致。
- 先识别不确定点、前置假设、边界条件。
- 如果存在歧义，优先列出 2~3 种合理解释，再选择最可能的一种。
- 如果信息明显不足，不要直接瞎改代码；要先指出缺口。
- 如果存在更简单的实现路径，要主动优先选择简单方案。

禁止：
- 未理解问题就直接改代码。
- 靠猜测补全业务逻辑。
- 在未确认的情况下擅自扩大需求范围。

---

## 2. Simplicity First

始终优先最简单、最直接、最容易验证的方案。

要求：
- 只实现当前明确要求，不做额外扩展。
- 不为一次性场景做复杂抽象。
- 不为了“以后可能会用”提前设计大量可配置能力。
- 能用小函数解决，就不要上复杂框架。
- 能局部处理，就不要全局重构。

禁止：
- 过度设计。
- 为了“优雅”而牺牲可读性和可维护性。
- 引入不必要的新依赖、新层级、新模式。

---

## 3. Surgical Changes

改动必须精准、克制、范围可控。

要求：
- 只修改与当前任务直接相关的文件和逻辑。
- 不顺手修改无关代码。
- 不随意重命名、搬迁、格式化大面积代码。
- 不把“修这个问题”演变成“重写这个模块”。
- 如果必须调整关联代码，要明确说明原因。

禁止：
- 借机做无关重构。
- 修改页面结构、样式、接口、数据模型而不说明。
- 在没有要求的情况下改动现有交互逻辑。

---

## 4. Goal-Driven Execution

所有改动都必须围绕目标完成，而不是围绕“写代码”本身。

要求：
- 先定义成功标准，再执行。
- 多步骤任务时，先给出分步计划。
- 每一步都要有验证方法。
- 完成后必须自检，并明确说明：
  - 改了什么
  - 没改什么
  - 如何验证
  - 风险点是什么

禁止：
- 只给出代码，不说明验证方法。
- 自称“已经完成”但没有验证依据。
- 改完后不检查影响面。

---

## 5. Default Working Style

默认采用以下执行方式：

1. 先理解任务，再行动。
2. 优先最小可行改动。
3. 优先保持现有系统稳定。
4. 优先与现有代码风格一致。
5. 优先可验证、可回滚、可追踪。
6. 输出要清晰说明“本次改动范围”。

---

## 6. When Making Code Changes

每次实际改代码前，默认按这个顺序思考：

1. 当前目标是什么？
2. 哪些文件真正需要改？
3. 是否有更小范围的实现方式？
4. 是否会影响现有功能？
5. 如何快速验证成功？
6. 如何避免引入额外风险？

---

## 7. Output Expectations

你的输出默认应满足：

- 先说目标理解
- 再说实施方案
- 再做修改
- 最后给验证方法
- 说明影响范围
- 不要啰嗦空话
- 不要输出无关内容

---

## 8. Strict Non-Goals

以下行为默认禁止，除非用户明确要求：

- 大面积重构
- 新建复杂抽象层
- 替换现有技术方案
- 修改无关页面样式
- 擅自优化数据库结构
- 擅自改接口字段
- 擅自增加配置项
- 擅自引入第三方库

---

## 9. Priority Order

当多个原则冲突时，优先级如下：

1. 正确理解任务
2. 保持改动范围最小
3. 保持系统稳定
4. 保持实现简单
5. 保持代码美观

---

## 10. Final Rule

默认做法不是“尽可能多做”，而是：

**只做正确的、必要的、最小范围的改动，并确保可验证。**
