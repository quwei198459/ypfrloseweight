---
alwaysApply: true
---
---
description: UI fidelity rules for screenshot-based and design-driven page implementation
globs: ["frontend/**/*", "designs/**/*", "screenshots/**/*", "material/**/*"]
alwaysApply: true
---

# UI Fidelity Rules

本项目中的页面开发、静态页面生成、设计稿修复、截图复刻，必须优先保证高保真。

## 1. Fidelity First

页面实现优先级：

1. 与设计稿一致
2. 与原截图一致
3. 与现有交互逻辑一致
4. 与现有代码风格一致

禁止：
- 自行发挥
- 擅自“互联网化重设计”
- 擅自更换布局逻辑

---

## 2. Structure Preservation

在没有明确要求的情况下：

- 不改 DOM / 组件结构层级
- 不改页面模块顺序
- 不合并或拆分原有区块
- 不改动页面跳转关系

如果必须改结构，必须说明：
- 原因
- 影响范围
- 为什么不能只改样式

---

## 3. Style Change Boundaries

允许修改的内容仅限于任务明确要求的样式项，例如：

- 间距
- 对齐
- 字号
- 圆角
- 边框
- 背景色
- 图标位置
- 局部高度宽度

禁止：
- 整体重做视觉风格
- 批量统一替换组件
- 改动无关页面样式

---

## 4. Screenshot-Driven Repair Mode

当任务基于截图进行修复时：

要求：
- 优先逐项对比差异
- 一次只解决明确列出的差异
- 不要在同一轮中混入额外改动
- 每个差异都要说明修改点

输出时应包含：
- 差异点清单
- 对应修改项
- 验证方式

---

## 5. Design-to-Code Discipline

从设计稿生成前端时：

要求：
- 先确定页面结构，再生成代码
- 组件命名要清晰、语义化
- 不要为了生成方便而篡改设计结构
- 如果设计稿存在不合理之处，要先标记，不直接瞎实现

禁止：
- 设计稿和代码脱节
- 生成后的页面与设计稿差异过大
- 用大量硬编码去“糊”出视觉效果但无法维护

---

## 6. Interaction Preservation

如果页面已有交互：

- 不得删除点击行为
- 不得改变原有跳转意图
- 不得把两个点击区合并成一个
- 不得因样式调整误伤交互热区

---

## 7. Verification Requirements

页面改动后必须给出至少一种验证方法，例如：

- 与设计稿逐项比对
- 与截图逐项比对
- 运行页面手工检查
- 检查点击区域和弹层逻辑
- 检查滚动、吸顶、固定按钮等行为

---

## 8. Default Rule

默认不是“把页面做得更漂亮”，而是：

**把页面做得更接近目标设计，并且不破坏已有结构和交互。**