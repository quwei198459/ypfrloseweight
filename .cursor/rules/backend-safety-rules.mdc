---
alwaysApply: true
---

---
description: Backend safety rules for Spring Boot, database, API, and integration changes
globs: ["backend/**/*", "database/**/*"]
alwaysApply: true
---

# Backend Safety Rules

后端改动默认属于高风险改动，必须遵守最小影响原则。

## 1. Safety First

优先级如下：

1. 不破坏现有接口
2. 不破坏现有数据
3. 不破坏现有业务流程
4. 再考虑新增能力

---

## 2. API Change Discipline

修改接口时必须明确说明：

- 哪个接口改了
- 改了哪些字段
- 是否向前兼容
- 哪些前端页面会受影响
- 如何验证

禁止：
- 无说明改接口结构
- 无说明改状态码语义
- 无说明改分页、排序、过滤规则

---

## 3. Database Change Discipline

修改数据库时必须明确：

- 新增/修改/删除了哪些表或字段
- 字段类型、默认值、约束
- 索引是否变化
- 是否影响历史数据
- 是否需要迁移脚本
- 初始化数据是否需要同步更新

禁止：
- 直接改表而无脚本
- 在代码中隐式依赖数据库变更
- 不说明字段兼容性就强行上线

---

## 4. Business Logic Discipline

修改业务逻辑时：

要求：
- 先解释当前逻辑
- 再说明目标逻辑
- 再说明差异点
- 再说明影响范围

禁止：
- 直接重写整段逻辑
- 无说明改状态流转
- 无说明改关键计算规则

---

## 5. Integration Discipline

前后端联调相关改动时：

要求：
- 明确接口入参与出参
- 明确字段命名是否一致
- 明确空值、异常、默认值处理
- 明确前端依赖字段是否存在

---

## 6. Logging and Debugging Discipline

允许：
- 为当前任务增加必要日志
- 为当前排查增加临时调试代码

但要求：
- 调试代码必须可回收
- 不长期保留噪音日志
- 不把敏感信息输出到日志

---

## 7. Verification Requirements

后端改动完成后必须给出验证路径，例如：

- 接口调用方式
- 示例请求参数
- 示例返回结果
- 数据库检查 SQL
- 关键业务流程测试步骤

---

## 8. Strict Non-Goals

除非明确要求，否则禁止：

- 大面积重构 service / controller / mapper
- 替换 MyBatis-Plus 使用方式
- 重构整个表结构
- 改动通用响应封装
- 改动全局异常处理
- 改动认证鉴权体系

---

## 9. Default Rule

默认做法是：

**在不破坏现有系统的前提下，用最小改动完成当前后端目标。**